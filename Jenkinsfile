pipeline {
    agent any
    stages {
        stage("INIT") {
            steps {
                sh 'ls'
            }
        }
        stage("PLAN") {
            steps {
                sh 'pwd'
            }
        }
    }
}